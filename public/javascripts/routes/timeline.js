(function (global, $, _, util) {
    'use strict';

    var viewmodel = util.namespace('ImageStreaming.viewmodel'),
        timeline = new viewmodel.Timeline();

}(window, jQuery, _, window.nakazawa.util));